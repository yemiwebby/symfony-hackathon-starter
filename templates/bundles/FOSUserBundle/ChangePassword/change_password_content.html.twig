{% trans_default_domain 'FOSUserBundle' %}


<div class="container">
    <div class="row">
        <div class="col-md-12 title_header text-center">
            <h3> Change password </h3>
        </div>

        <div class="col-md-12">

            <div class="text-center mb-4">
                <h1 class="h3 mb-3 font-weight-normal">Symfony Hackathon Starter </h1>
            </div>

            {{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password form-signin' } }) }}

            <div class="form-label-group">
                {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
                {{ form_label(form.email) }}
            </div>


            <div class="form-label-group">
                <button type="submit" id="_submit" name="_submit" class="btn btn-lg btn-primary btn-block" type="submit">{{ 'change_password.submit'|trans }}</button>
            </div>
            {{ form_end(form) }}

        </div>
    </div>
</div>

